CREATE TABLE func(
	nome varchar(20),
	cpf int,
	rg varchar(9),
	numero int,
	setor varchar(10),
	estadocivil varchar(20),
	salario int,
	gestor varchar(20),
	cargo varchar(10),
	datanascimento date,
	endereco varchar(20),
	PRIMARY KEY (cpf)
);
